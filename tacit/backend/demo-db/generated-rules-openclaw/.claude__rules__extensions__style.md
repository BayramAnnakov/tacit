---
description: Style conventions
---

- The project requires Node >= 22.12.0. Use `Array.prototype.toSorted()` instead of `[...arr].sort()` — the linter enforces using `toSorted()` for immutable array sorting.
