---
description: Domain conventions
---

- Canvas and A2UI: Canvas host (default port 18793) serves agent-editable HTML. A2UI enables agent-driven visual workspace with push/reset, eval, and snapshot commands. Available on macOS/iOS/Android nodes via node.invoke. Agents can control the canvas surface programmatically.
