---
description: Domain conventions
---

- Agent-to-agent coordination via sessions_* tools: sessions_list (discover active sessions/agents and metadata), sessions_history (fetch transcript logs), sessions_send (message another session with optional reply-back ping-pong and announce step). Off by default; must be explicitly enabled and allowlisted via tools.agentToAgent.enabled and tools.agentToAgent.allow.
